<li id="kaup-hidden">
    <p class="display-block"><a class="haup-link">Equipment mieten / hey! rentme</a></p>
    <span id="close-haup" class="haup-close-icon"></span>
    {% for cat in categories %}
        <p>
            <a href="{{ path('catchall', { 'content': cat.slug }) }}#categories"><img src="{{ cat.image.urlPath(image_url_prefix) }}" alt=""/></a>
            <a href="{{ path('catchall', { 'content': cat.slug }) }}#categories">{{ cat.Name }}</a>
        </p>
    {% endfor %}
</li>
